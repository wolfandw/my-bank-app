global:
  namespace: test

accounts-chart:
  replicaCount: 2
  resources:
    limits: { memory: "1Gi", cpu: "1000m" }
    requests: { memory: "512Mi", cpu: "200m" }

cash-chart:
  replicaCount: 2
  resources:
    limits: { memory: "1Gi", cpu: "1000m" }
    requests: { memory: "512Mi", cpu: "200m" }

notifications-chart:
  replicaCount: 2
  resources:
    limits: { memory: "1Gi", cpu: "1000m" }
    requests: { memory: "512Mi", cpu: "200m" }

transfer:
  replicaCount: 2
  resources:
    limits: { memory: "1Gi", cpu: "1000m" }
    requests: { memory: "512Mi", cpu: "200m" }

frontui-chart:
  replicaCount: 2
  resources:
    limits: { memory: "1Gi", cpu: "1000m" }
    requests: { memory: "512Mi", cpu: "200m" }

gateway:
  replicaCount: 2
  resources:
    limits: { memory: "1Gi", cpu: "1000m" }
    requests: { memory: "512Mi", cpu: "200m" }

accountsdata: { postgres: { password: "postgres" } }
cashdata: { postgres: { password: "postgres" } }
notificationsdata: { postgres: { password: "postgres" } }
transferdata: { postgres: { password: "postgres" } }