apiVersion: apps/v1
kind: Deployment
metadata:
  name: { { .Chart.Name } }
  namespace: { { .Release.Namespace | default "dev" } }
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: { { .Chart.Name } }
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: my-bank-app-config
          env:
            - name: NOTIFICATIONS_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: my-bank-app-secrets
                  key: NOTIFICATIONS_DB_USERNAME
            - name: NOTIFICATIONS_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: my-bank-app-secrets
                  key: NOTIFICATIONS_DB_PASSWORD
            - name: NOTIFICATIONS_DB_HOST
              value: "my-bank-app-chart-notificationsdata"
            - name: NOTIFICATIONS_DB_PORT
              value: "5436"
            - name: NOTIFICATIONS_DB_NAME
              value: "notifications_db"
            - name: KEYCLOAK_NOTIFICATIONS_SECRET
              valueFrom:
                secretKeyRef:
                  name: my-bank-app-secrets
                  key: KEYCLOAK_NOTIFICATIONS_SECRET
            - name: KEYCLOAK_HOST
              value: "keycloak"
            - name: KEYCLOAK_PORT
              value: "8080"
            - name: KEYCLOAK_REALM
              value: "my-bank-app-realm"