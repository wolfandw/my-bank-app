apiVersion: v1
kind: Service
metadata:
  name: {{ include "transfer-chart.fullname" . }}
  labels:
    {{- include "transfer-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transfer-chart.selectorLabels" . | nindent 4 }}
