apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontui-chart.fullname" . }}
  labels:
    {{- include "frontui-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontui-chart.selectorLabels" . | nindent 4 }}
