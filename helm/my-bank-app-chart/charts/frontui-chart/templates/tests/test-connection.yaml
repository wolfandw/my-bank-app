apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontui-chart.fullname" . }}-test-connection"
  labels:
    {{- include "frontui-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontui-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
