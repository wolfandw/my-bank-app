apiVersion: apps/v1
kind: Deployment
metadata:
  name: { { .Chart.Name } }
  namespace: { { .Release.Namespace | default "dev" } }
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: { { .Chart.Name } }
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: my-bank-app-config
          env:
            - name: KEYCLOAK_GATEWAY_SECRET
              valueFrom:
                secretKeyRef:
                  name: my-bank-app-secrets
                  key: KEYCLOAK_GATEWAY_SECRET
            - name: KEYCLOAK_HOST
              value: "keycloak"
            - name: KEYCLOAK_PORT
              value: "8080"
            - name: KEYCLOAK_REALM
              value: "my-bank-app-realm"