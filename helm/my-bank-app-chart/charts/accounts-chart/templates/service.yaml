apiVersion: v1
kind: Service
metadata:
  name: {{ include "accounts-chart.fullname" . }}
  labels:
    {{- include "accounts-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "accounts-chart.selectorLabels" . | nindent 4 }}
